/// Planned real-product acceptance contract for multi-device MLS status UI.
///
/// These cases remain out of `suite_manifest.json` until AWiki Me and a CLI
/// device use independent native Core roots against the deployed P6 v2 path.
/// Widget fakes cannot prove distinct Leafs, epoch advancement, or revoke.
const plannedMlsMultiDeviceUiCaseIds = <String>[
  'MLS-MULTI-DEVICE-E2E-001',
  'MLS-MULTI-DEVICE-E2E-002',
];
