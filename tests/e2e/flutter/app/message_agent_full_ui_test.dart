import 'app_smoke_test.dart' as app_smoke;

// Step 01 wires the durable Flutter tooling entry only. Later Message Agent
// steps must replace this with product-specific full UI assertions.
void main() => app_smoke.main();
