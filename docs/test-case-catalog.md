# AWiki Me test case catalog

> Generated from `tests/e2e/case_catalog.json`; do not edit by hand.
> Catalog revision: `2026-07-29.dirty-hint-v2-v1`.

Every row is reconciled with `tests/e2e/suite_manifest.json`. The checker requires a unique ID, complete ownership/environment/cleanup metadata, an existing implementation path containing the ID, and exact report IDs.

## Audited cases

| Case | Feature and action | Exact oracle | Negative guard | Gate / environment | Implementation |
|---|---|---|---|---|---|
| `ATTACH-E2E-001` (active) | **App to CLI attachment**<br>Stage an attachment through desktop drop UI and send it to CLI. | • The desktop drop uses a real temporary filesystem source and both the staged draft model and visible preview preserve the exact filename.<br>• One canonical attachment message reaches terminal sent state.<br>• CLI verifies filename, MIME, size, digest and downloaded bytes exactly. | • Service-only staging, metadata mismatch, byte/digest mismatch, or duplicate message fails. | suites: `attachment, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-attachments+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `ATTACH-E2E-002` (active) | **CLI to App attachment**<br>Send an attachment from CLI and open/download it through App UI. | • App observes one canonical attachment message.<br>• Filename, MIME, size, digest and downloaded bytes match exactly. | • Wrong conversation, missing bytes, metadata mismatch, or duplicate render fails. | suites: `attachment, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-attachments+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `ATTACH-REG-001` (active) | **Attachment integrity regression**<br>Requery both attachment directions after the stability window. | • Canonical IDs remain unique and content digests/bytes remain unchanged. | • Replay duplicate, digest drift, wrong object ticket, or lost attachment fails. | suites: `attachment, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-attachments+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `AUTH-E2E-001` (active) | **Remote identity/auth preflight**<br>Prepare the App identity, activate the authenticated shell, and cross-check App/CLI handle resolution. | • App session is authenticated.<br>• CLI current DID equals directory resolution of its configured handle.<br>• App and CLI canonical identities are distinct. | • Stale handle mapping, wrong tenant/domain, or identity equality fails before any message action. | suites: `attachment, contacts, direct, display-name-fallback, full, group, inbound`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CLAUDECODEAGENT-E2E-001` (active) | **Claude Code runtime creation**<br>Create/select a Claude Code runtime Agent and open chat through UI. | • The committed daemon create status converges the Agent page without a manual inventory refresh.<br>• One canonical runtime Agent is selected and its chat is visible. | • Missing/disabled UI, skipped real scenario, manual refresh dependency, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `claude-code-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/claude_code_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CLAUDECODEAGENT-E2E-002` (active) | **Claude Code prompt send**<br>Enter and send the deterministic prompt through UI. | • Visible send action completes for the exact prompt. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `claude-code-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/claude_code_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CLAUDECODEAGENT-E2E-003` (active) | **Claude Code runtime completion**<br>Wait for daemon runtime_run and final outbox. | • One run reaches finished and one final outbox record reaches sent. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `claude-code-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/claude_code_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CLAUDECODEAGENT-E2E-004` (active) | **Claude Code reply visibility**<br>Sync App history and wait for the visible reply. | • Exact expected reply appears once in canonical history and UI. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `claude-code-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/claude_code_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CODEXAGENT-E2E-001` (active) | **Codex runtime creation**<br>Create/select a Codex runtime Agent and open its chat through UI. | • The committed daemon create status converges the Agent page without a manual inventory refresh.<br>• One canonical runtime Agent is selected and its chat is visible. | • Missing/disabled UI, skipped real scenario, manual refresh dependency, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `codex-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/codex_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CODEXAGENT-E2E-002` (active) | **Codex prompt send**<br>Enter and send the deterministic prompt through UI. | • Visible send action completes for the exact prompt. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `codex-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/codex_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CODEXAGENT-E2E-003` (active) | **Codex runtime completion**<br>Wait for daemon runtime_run and final outbox. | • One run reaches finished and one final outbox record reaches sent. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `codex-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/codex_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CODEXAGENT-E2E-004` (active) | **Codex reply visibility**<br>Sync App history and wait for the visible reply. | • Exact expected reply appears once in canonical history and UI. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `codex-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/codex_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `CONTACT-E2E-001` (active) | **App follow interaction**<br>Open the exact follower contact and tap the visible follow control in the resulting Direct peer information surface. | • Button is visible and enabled.<br>• Backend relationship becomes exactly friend and both directional lists contain the exact peer once. | • Missing/disabled/inert click, backend error, or broad friend/follower acceptance fails. | suites: `contacts, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-contacts+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONTACT-E2E-002` (active) | **CLI follow and App unfollow interaction**<br>Let CLI follow the App identity, refresh, then tap App unfollow. | • CLI follow completes.<br>• App observes exact follower/friend convergence and unfollow returns the relation to none. | • UI-only state, wrong peer, nonconverged backend state, or failed tap fails. | suites: `contacts, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-contacts+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONTACT-FIRST-CONV-E2E-001` (active) | **Contact-first canonical Direct creation**<br>Open the exact follower row before using identity lookup, recents, local search, or sending a first message. | • The visible contact row uses the deterministic nickname and opens one dm:peer-scope:v1 conversation with non-empty peerPersonaId.<br>• The empty Direct remains exact-one by Persona before its first message and subsequent contact re-entry reuses the same canonical conversation ID. | • A pre-existing Direct fixture, Handle/DID title drift, legacy dm:<DID> row, second Persona row, wrong peer, or a disappearing empty conversation fails. | suites: `contacts`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-contacts+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONTACT-MSG-E2E-001` (active) | **Contact row canonical Direct message closed loop**<br>Click the exact visible contact row, send one run-unique message through the App, restart, then receive and read one run-unique CLI message. | • The exact contact row displays the same nickname-first title as identity lookup and opens the existing dm:peer-scope:v1 conversation for the same Persona.<br>• CLI inbox/history contain exactly one matching sender, receiver, body, type, message id, and thread.<br>• Core conversation summary, visible row/header, timeline message, and Product overlay each have exactly one canonical identity with no dm:<DID> overlay.<br>• Restart preserves one canonical row/message; closed-view incoming unread is exactly +1, restart preserves 1, and opening clears it to 0 without rebound. | • A legacy DID row/overlay, duplicate message/summary/UI row, wrong peer/content/type, nonterminal send, missing restart restoration, or inaccurate unread/read state fails. | suites: `contacts, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-contacts+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONTACT-REG-001` (active) | **Relationship transition/idempotency regression**<br>Exercise the canonical relationship lifecycle through refreshes and repeated reads. | • The exact sequence none→follower→friend→following→friend→follower→none is observed.<br>• CLI status derives the combined state from all directional booleans while independently checking the outbound relationship field; refresh does not invent duplicate or stale relations. | • Any broadened state set, duplicate relation, or stale post-refresh state fails. | suites: `contacts, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-contacts+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONV-CANON-E2E-001` (active) | **Canonical Direct conversation across App entries**<br>Create the Direct through identity lookup before the first message, then reopen it through recents, local conversation search, restart, and the visible Contact row. | • Every App entry reuses the same peer-scope canonical conversation ID and peer Persona ID.<br>• The empty pre-message conversation remains visible, then messages, preview, unread, and cached nickname stay attached to that single row across restart. | • A legacy dm:<DID> row, a second semantic row, a changed Persona/conversation ID, a lost empty row, or entry-specific title/state fails. | suites: `direct, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `CONV-LIST-E2E-001` (active) | **Multi-conversation list projection correctness**<br>While both conversations are closed, deliver Direct, Group, then Direct messages and restart before inspecting the list. | • After each delivery, App projection relative order follows the latest canonical message without duplicating either semantic conversation.<br>• After restart, visible Direct and Group rows have exact nickname/group title, exact latest preview, exact unread badge, and Direct appears before Group. | • Duplicate semantic row, stale row order, wrong title/preview, missing row, or zero-unread badge fails immediately. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `DEVICE-ACCOUNT-DOMAIN-ISOLATION-E2E-001` (active) | **One failed User domain does not block Profile or ordinary message convergence**<br>Arm one Inventory read failure for the joining device, mutate Agent Inventory and Profile, send an ordinary Direct message, reconcile once, then retry normally. | • The first reconcile reports only Agent Inventory as failed while Profile advances and renders the exact new value.<br>• The ordinary Direct message converges exactly while Inventory is failed, proving message sync is not blocked by a User domain.<br>• The prior Inventory snapshot remains visible until a later retry pulls the higher version and clears the domain error. | • A wildcard or reusable failpoint, operator receipt used as a business oracle, cleared prior cache, blocked Profile/message sync, more than one failed domain, or pass before the normal retry converges prevents attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-ADD-SYNC-E2E-001` (active) | **A runtime Agent added on one App converges to the joined sibling App**<br>Create Codex and Claude Code runtime Agents through the admin App product controller, then let the joining App reconcile only through its Account State coordinator. | • The joining App consumes a higher Agent Inventory version and projects the exact Daemon/runtime parent topology.<br>• Each added runtime DID appears exactly once with the expected Handle, runtime kind, and display name. | • Copied local state, a direct test-side cache write, duplicate Agent rows, mismatched parent topology, or attestation before the joining App projection converges prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-ARCHIVE-SYNC-E2E-001` (active) | **A daemon-backed runtime delete archives an independent Agent across Apps**<br>Select that runtime in the product Agents controller and execute the normal daemon-backed runtime delete, whose authoritative remote lifecycle result is archived, then reconcile the sibling App. | • The independent runtime is created through the product daemon path and is visible as active on the sibling before deletion.<br>• The writing App submits the daemon-backed runtime delete rather than a test operator, direct storage write, or reuse of the separate Codex deletion.<br>• The durable full Inventory snapshot contains exactly one archived row for the archived Agent DID at the higher version.<br>• The visible Agents projection excludes the archived row. | • A test-only operator, direct storage mutation, reuse of the separate Codex deletion, physical row loss, visible archived Agent, duplicate row, or writing-side-only assertion prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-DELETE-SYNC-E2E-001` (active) | **A real runtime delete completes on the writing App and converges to its sibling**<br>Submit runtime deletion through the admin App controller and wait for the service-backed terminal reconcile before accepting sibling completion. | • The admin App submits the real runtime delete and removes the target only after authoritative reconcile.<br>• The joining App confirms terminal convergence for the exact Agent DID. | • Local optimistic removal alone, a Daemon command failure, a retained visible runtime, or treating a UI disappearance without sibling confirmation as delete success prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-MESSAGE-SYNC-E2E-001` (active) | **A joined App sends a default-plain runtime Agent prompt that converges on the other App**<br>On the joining App, select the existing Codex runtime Agent, open its chat through visible UI, send one exact prompt through the real composer, then observe the exact canonical own-sync message on the admin App. | • The joining App opens the already-converged runtime Agent chat and commits the exact prompt through the visible composer as a sent default-plain Direct message with the account DID and runtime Agent DID.<br>• The admin App projects the same canonical conversation and exact message ID as isMine=true and visibly renders the prompt. | • Enabling Direct E2EE, calling MessagingService directly on the joining App, creating a test-only conversation, duplicate content, canonical ID mismatch, wrong sender or receiver, failed send state, missing own-sync semantics, or a non-visible admin-App message prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-agents+awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-RENAME-SYNC-E2E-001` (active) | **A runtime Agent rename converges independently across Apps**<br>Rename that runtime through the admin App product controller and reconcile the joining App. | • The joining App Agent Inventory version increases and the same Agent DID retains its topology while exposing the exact new display name.<br>• The joining App renders exactly one renamed Agent. | • A changed Agent DID, duplicate row, stale old name, direct cache mutation, or a name-only assertion without the higher Inventory version prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-SYNC-E2E-001` (active) | **Two joined AWiki Me Apps converge the same account Agent inventory**<br>Open the Agents workspace on the joining App before the admin App installs the Daemon and creates one Codex plus one Claude Code runtime Agent, then wait only on the joining App provider/UI convergence path. | • Both Apps project the exact same Daemon DID and Handle from the account-level User Service Agent Inventory.<br>• Both Apps project the exact same Codex and Claude Code runtime DIDs, Handles, runtime kinds, and parent Daemon DID.<br>• The joining App renders both runtime display names without the test directly invoking its inventory refresh after creation. | • A copied App/Core root, a second Daemon installation, a test-side inventory refresh after runtime creation, duplicate Agent rows, mismatched DIDs/Handles/parent topology, or either runtime name not becoming visible prevents pass attestation.<br>• This case cannot attest macOS LocalAuthentication because the functional suite intentionally uses an E2E-only UserPresencePort. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-AGENT-UNBIND-SYNC-E2E-001` (active) | **Agent unbind converges as an inactive topology fact**<br>Unbind the exact runtime through the admin App product controller, then reconcile the joining App. | • The joining App durable Inventory snapshot retains exactly one inactive row for the unbound Agent DID at a higher version.<br>• The joining App visible Agent projection removes that inactive Agent. | • Physical disappearance from the durable snapshot, a still-visible inactive Agent, duplicate rows, or inferring unbind only from the writing App prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-JOIN-E2E-001` (active) | **AWiki Me new-device Join with CLI admin**<br>Bootstrap the existing CLI admin, start its real listener, start Join through the real AWiki Me onboarding UI, restart the App while Join is pending, require the CLI listener to emit the redacted im.device.join.requested event before reading the local Core projection or starting verification, compare the independently displayed six-digit SAS, approve through the foreground CLI as a member, and wait for AWiki Me to re-resolve authorization. | • The independently bootstrapped CLI admin and App requester use distinct fresh native Core roots; the OTP-authenticated App request remains pending before the admin starts verification, an App restart restores that same pending session without persisting SAS, and the listener emits exactly the dedicated redacted Join wake before any admin mutation.<br>• The App-visible and CLI-derived six-digit SAS match without recording either value, and the production foreground CLI contract approves the fixed member role without an admin-role choice.<br>• Both Registries contain exactly two devices: AWiki Me is the current active member with management_ready false only after authoritative re-resolution, while the CLI remains the current active management-ready admin. | • A fake provider, copied state, static OTP, unsafe resolver argv, malformed SMS-error response, missing listener host wake, direct Message Inbox hydration, unapproved host-event fields, split claim or admin polling, missing pending phase, changed or terminal Join after restart, persisted SAS, mismatched SAS/session/device, non-foreground approval, retained terminal SAS, admin role, or server success without final Registry authorization prevents pass attestation. | suites: `multi-device-remote-join`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-JOIN-E2E-002` (active) | **AWiki Me admin-side approval defaults to member**<br>Bootstrap AWiki Me as the ready admin, start an independent CLI Join request while the App is on its normal authenticated shell, wait for system_notification_changed to refresh the Core Join inbox and expose the global review entry, open that entry to start verification exactly once, compare the independently derived six-digit SAS, approve the fixed member role through one real operating-system user-presence prompt, and poll the new-device result. | • The independently bootstrapped App admin and CLI requester use distinct fresh native Core roots; the OTP-authenticated request remains pending until the App receives its system notification, refreshes the typed Core Join inbox independently of general message sync, exposes the global review entry, and the user explicitly starts verification.<br>• The App-visible and CLI-derived six-digit SAS match without recording either value, and exactly one delegated real user-presence confirmation succeeds.<br>• Both Registries contain exactly two devices: the joining protocol_device_id is the CLI current active member with management_ready false, while the original App device remains the unique current active management-ready admin. | • A fake provider, copied or non-fresh local state, static OTP, unsafe resolver argv, unapproved or malformed SMS-error response, Registry discovery, direct requestSync/refreshJoinInbox calls, pre-opening Devices to bypass the global entry, split claim or admin polling, repeated verification start, secret-bearing evidence, mismatched SAS, an enabled approval action before SAS confirmation, any admin-role control, a denied or repeated user-presence result, mismatched protocol_device_id, or an active-admin joining result prevents pass attestation. | suites: `multi-device-remote-join`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-JOIN-E2E-003` (planned) | **AWiki Me Join restart and terminal-state convergence**<br>Restart AWiki Me during Join, resume from Core state, then exercise cancellation and expiration as separate isolated runs. | • The resumed screen projects the same session without persisting SAS in App state, and cancellation or expiration reaches one terminal state without authorizing a device. | • A duplicated device, stale approval action, raw token or OTP in reports, a persisted SAS, silent retry authorization, or a false passed attestation fails. | suites: `planned`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_session_and_identity_cleanup`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-JOIN-E2E-004` (active) | **Two isolated AWiki Me Apps complete member Join on one macOS host**<br>Launch and concurrently drive one admin App and one joining App, start Join through the joining App UI, require the authenticated admin runtime to expose the listener-delivered global review entry without test-side inbox refresh, explicitly start verification, compare both locally rendered SAS values only in the loopback coordinator memory, approve through one real operating-system user-presence prompt, and wait for both Registry projections. | • Two real AWiki Me processes use distinct bundle identifiers, build products, and fresh native Core roots; the joining App remains pending without SAS until the admin user explicitly starts verification from the global Join review entry.<br>• The two App-rendered six-digit SAS values match only inside the authenticated loopback coordinator, no SAS is written to config, logs, attestation, or reports, and exactly one real user-presence confirmation succeeds.<br>• Both App Core Registries converge to exactly the same two devices: the original App remains the management-ready admin and the joining App becomes the current active rootless member. | • A copied/shared state root, shared bundle or build product, fake Core, direct requestSync/refreshJoinInbox calls, pre-opened Devices page, implicit or repeated verification, persisted or reported SAS, mismatched SAS/session/device, approval before explicit SAS confirmation, denied or repeated user presence, admin-role joining result, or either Registry failing exact two-device convergence prevents pass attestation. | suites: `multi-device-app-pair`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-DIAGNOSTICS-E2E-001` (active) | **Real recovery exposes only typed product-safe sync diagnostics**<br>Observe the product coordinator across recovery, then inspect only its typed diagnostics projection after the reliable chain completes. | • Recovery-required, recovering, or Core recovering mode is observed and the coordinator returns to a successful idle state.<br>• Diagnostics expose a non-negative pending mutation count, closed dirty-domain/retry values, and last-success time.<br>• The product refresh attempt and success sequences both advance after recovery, are equal, and mark the serialized observation current.<br>• The E2E-safe diagnostics projection contains no cursor, epoch, account/device identifier, recovery token, message content, or payload field. | • A test-created allowlist map, stale diagnostics retained after refresh failure, reading raw SQLite, serializing IDs/cursors/content, accepting a negative count, or treating catalog text as runtime evidence prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-GENERATION-FENCE-E2E-001` (active) | **Same-DID revoked device is auth-fenced from later reliable sync**<br>Capture the exact joined-device account/auth binding, revoke that device, commit a new ordinary Direct message for the account, verify the original local binding remains in place, then trigger one normal reliable pull. | • The real Registry mutation revokes the exact same-DID device after the E2E captured its original account and device-auth binding, before the post-revoke message is committed.<br>• The revoked App does not project the post-revoke committed message before or after its subsequent reliable pull, and that pull ends in product auth fencing. | • Claiming direct stale-Patch injection, forging a binding, projecting the post-revoke message, allowing the revoked token to pull, revoking a different device, or running another shared-account case afterward prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging+awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-HINT-LOSS-E2E-001` (active) | **A joined AWiki Me replica converges an ordinary message after every realtime hint is lost**<br>Quiesce sync, pause the admin App and stop its realtime service, commit one ordinary Direct message from the CLI peer while no WebSocket can observe its hint, then resume through the normal App lifecycle. | • The realtime service is stopped before the peer message is committed and the local timeline remains unchanged before lifecycle resume.<br>• App resume starts the normal reliable HTTP reconciliation path and projects the exact canonical peer message once.<br>• A repeated local timeline read keeps one projection with the same canonical message ID. | • Using a raw WebSocket payload as message truth, a test-side cursor or sync call, observing the message before resume, missing or duplicating the reliable projection, changing its canonical ID, or relying on Push prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-OFFLINE-RECOVERY-E2E-001` (active) | **An existing AWiki Me replica resumes through Core-owned bounded compact recovery**<br>Take the existing joining App offline, create one ordinary Direct message and a read-state change, use the fixed operator-only helper to create a retention gap for that exact joined device, then resume it through normal App lifecycle/reconnect triggers and observe the single high-level syncNow recovery flow without supplying any recovery control to the App. | • The coordinator transiently reports recovery-required/recovering or completes recovery within one Core syncNow chain, then returns to idle without exposing cursor, token, cutoff, or count.<br>• Recent ordinary messages converge exactly once from the Core committed projection while previously available older local messages remain available.<br>• Current read state converges independently of the ordinary-message recovery window, and a post-anchor ordinary Direct message arrives exactly once through the following delta. | • An invalid operator receipt, App recover API, App-supplied cursor/token/cutoff/count, full-history replay, deletion of older local rows, duplicate post-anchor delivery, raw Snapshot merging in Flutter, auth-revoked background retry, or any E2EE/MLS history prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-ONLINE-SYNC-E2E-001` (active) | **Two online joined AWiki Me replicas converge ordinary Direct messages through sync v2**<br>Keep both Apps online, send ordinary Direct messages from each App to the CLI peer, then send one ordinary reply from the CLI peer while both App runtimes consume WebSocket only as a pull trigger. | • The admin App outbound message is projected exactly once as outgoing on the joining sibling device.<br>• The joining App outbound message is projected exactly once as outgoing on the admin sibling device.<br>• The same incoming CLI reply is projected exactly once on both online App devices under the same canonical conversation. | • A raw realtime payload directly writing list, timeline, Group state, or notification truth; duplicate event/hint rendering; wrong direction; canonical conversation drift; secure-mode activation; or only one online device receiving the reply prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-PATCH-READY-E2E-001` (active) | **Bound App startup establishes committed Patch readiness before reliable pull**<br>Complete normal App activation, inspect the product-owned payload-free startup ordering observation without calling the barrier from the test, receive the first post-Join sibling message, then repeat reliable HTTP sync. | • The normal bound startup records Patch subscription start, committed reset readiness, and first reliable sync start in that strict order without a test-owned barrier call, cursor, or projection.<br>• The first post-startup committed sibling message appears exactly once and a repeated reliable pull keeps exactly one canonical message. | • A post-bootstrap test call to ensurePatchReady, unbound session, test-owned checkpoint, direct cache write, missing or reordered startup observation, duplicate projection, or repeat full-refresh substitute prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-READ-SYNC-E2E-001` (active) | **A read watermark committed on one joined App converges monotonically on its sibling App**<br>Send one ordinary Direct message from the CLI peer, observe the unread projection on both Apps, open the canonical conversation on the joining App through visible UI, and let normal Core sync/realtime triggers converge the read watermark on the admin App. | • Both Apps project the exact incoming ordinary message once and advance the same canonical conversation unread state.<br>• Opening the joining App conversation commits a Core-owned read watermark without an App-owned checkpoint or direct read-state wire call.<br>• The admin App eventually projects the same or higher read watermark and its unread count does not regress after reconnect or duplicate sync. | • A raw App read cursor, direct read-state payload, E2EE or MLS event, lower remote watermark, duplicate message, canonical conversation mismatch, or test-side projection mutation prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-RECONNECT-E2E-001` (active) | **An active AWiki Me device normally reconnects and pulls its disconnect gap exactly once**<br>Perform a normal realtime stop, commit one ordinary Direct message during the disconnected interval, resume the App, wait for WebSocket reconnection, and observe reliable HTTP gap reconciliation. | • The ordinary WebSocket disconnect completes without revoking the device and the active device reconnects successfully.<br>• The message committed in the disconnect interval is obtained from reliable HTTP reconciliation.<br>• The canonical post-reconnect message is projected exactly once and remains exact-one on a repeated read. | • Treating revoked reconnect denial as normal reconnect evidence, changing device identity, direct projection from a hint, failing to reconnect, missing the disconnect gap, duplicate projection, or Push-based recovery prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-SYNC-E2E-001` (active) | **Direct messages sent on either joined App converge as own-sync on the other App**<br>Send one exact Direct text from the admin App to the CLI peer and observe it on the joining App, then send a second exact Direct text from the joining App through the same canonical conversation and observe it on the admin App. | • The admin App commits exactly one sent message with the account DID, peer DID, exact content, canonical message ID, and canonical conversation ID.<br>• The joining App projects the first message as one canonical isMine own-sync message and renders it in the selected conversation.<br>• The joining App signs and commits a second default-plain outbound message with its own joined-device signing key, the same account DID, and canonical conversation ID.<br>• The admin App projects the second message as the exact canonical isMine own-sync message and renders it under the same conversation. | • A direct provider refresh that manufactures either conversation, duplicate body delivery, canonical ID mismatch, wrong direction, wrong peer conversation, missing own-sync semantics, or a non-visible bubble prevents pass attestation.<br>• A joined-device outbound rejected because its Origin Proof keyid does not match its device signing private key prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-SYNC-E2E-002` (active) | **A remote Direct reply converges on both joined Apps**<br>Send one exact default-plain reply from the CLI peer and observe the same canonical incoming message on both App device histories plus the joining App conversation list and visible timeline. | • The admin App receives the exact CLI reply with the CLI DID, account DID, canonical reply ID, sent state, and isMine=false.<br>• The joining App updates the same peer conversation preview without creating a second conversation.<br>• The joining App receives the exact incoming reply with the same canonical ID and renders it in the already-open timeline. | • Unexpected secure-mode activation, duplicate reply delivery, canonical ID mismatch, wrong direction, a second peer conversation, only one App receiving the reply, or a non-visible reply prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-MESSAGE-TAIL-ONLY-E2E-001` (active) | **A newly joined AWiki Me replica bootstraps at the current ordinary-message tail**<br>Complete member Join, allow the joining App startup sync to finish without test-side cursor or sync calls, prove the pre-Join message is absent, then send one post-Join message through the existing App. | • The ordinary pre-Join message is committed and visible on the existing App before the new replica is authorized.<br>• The joining App completes bootstrap without exposing the pre-Join conversation or message.<br>• A post-Join ordinary message becomes visible exactly once on the joining App, proving bootstrap did not disable future delta. | • Supplying cursor zero, account, device, replica classification, recovery token, or a test-side sync call; receiving any pre-Join message/history; failing bootstrap; or missing/duplicating the post-Join message prevents pass attestation. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-PROFILE-SYNC-E2E-001` (active) | **A Profile mutation converges through the independent Profile domain**<br>Update nickname and bio through the admin App Profile controller, then reconcile the joining App through Account State v1. | • The joining App Profile version increases and its visible Profile projection matches the exact nickname and bio.<br>• Agent Inventory and Device Registry versions remain unchanged by the Profile-only mutation. | • Timestamp conflict resolution, legacy unversioned Profile refresh, unrelated domain version amplification, stale Profile UI, or accepting only the writing-device response prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-REGISTRY-SYNC-E2E-001` (active) | **Device Registry revoke converges to the active sibling and fences the revoked App**<br>Load a fresh security Registry on the admin App, revoke the exact joined member with E2E-only user-presence, reconcile the admin display cache, then make the revoked App issue a product Registry request. | • The still-active admin observes one exact revoked device in both the fresh Core Registry and a higher-version Account State display cache.<br>• The revoked App's subsequent Registry request is rejected by generation/token fencing. | • Authorizing from the display cache, revoking the current admin, a different target device, no Registry version advance, successful use of the revoked token, or running further shared-account cases after revoke prevents pass. | suites: `multi-device-app-pair-functional`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_residual_ledger`<br>owner: `awiki-me-account-state` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DEVICE-REVOKE-E2E-001` (active) | **AWiki Me permanent device revoke**<br>From the real Devices page, select the non-current device, accept the visible destructive confirmation, complete one fresh operating-system user-presence check, and wait for the authoritative Registry projection. | • The target becomes revoked and loses its destructive action only after the Core mutation and Registry refresh, while the current AWiki Me device remains the active management-ready admin. | • Self revoke, revoking the final ready admin, cancellation, missing or repeated user presence, button-only success, an active target after refresh, or internal proof/version/key material in UI or evidence fails. | suites: `step4-revoke-mls`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_multi_device_group_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `DISPLAY-NAME-E2E-001` (active) | **Nickname consistency across identity and conversation surfaces**<br>Resolve the peer, open Direct and Contacts, inspect the add-member and @ mention candidates, add the same peer to a Group, observe the system event and receive a Group message. | • Identity lookup first visible primary title, Direct row/header, and Contact row all use the exact nickname.<br>• Add-member candidate, @ mention candidate, Group member projection, member-added system event, and incoming sender title/fallback avatar seed/avatar URI all use the same exact Persona Profile projection. | • Handle, compact/full DID, Unknown, stale roster/conversation names, generic @ avatars, or any different primary identity projection while the nickname is available fails. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `DISPLAY-NAME-E2E-002` (active) | **Full-Handle fallback consistency across App surfaces**<br>Resolve the peer and exercise identity lookup, Direct, Contacts, group member, group system-event, and sender-label surfaces through the App. | • Identity lookup, Direct row/header, and Contact row use the exact full Handle when nickname is unavailable.<br>• Group member, member-added system event, and incoming sender label use the same exact full Handle. | • A bare local name, generated user name, compact/full DID, Unknown, mixed fallback, transient wrong first title, or later self-healing fails. | suites: `display-name-fallback`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `DISPLAY-NAME-E2E-003` (planned) | **DID-only fallback consistency across App surfaces**<br>Open identity lookup, Direct, Contacts, group member, group system-event, and sender-label surfaces for the DID-only actor. | • Every scoped App surface uses the DID fallback only when both nickname and Handle are unavailable. | • The actor must not appear as Unknown, a stale generated user name, a stale sender snapshot, or a mixed fallback across App surfaces. | suites: `planned`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `DISPLAY-NAME-E2E-004` (active) | **Explicit Profile refresh converges every App display surface**<br>Open the existing Direct, click the visible peer avatar to refresh Profile, then reopen identity lookup, recents, Contacts, group info, the member-added system event, and the existing group sender label. | • The peer-info title, Direct App projection, conversation row, chat header, identity lookup, and Contact row converge to the exact new nickname while reusing the same Persona and canonical conversation ID.<br>• The existing group member, member-added system event, and message sender label converge to the same exact new nickname without changing the canonical Group conversation. | • A stale old nickname, Handle/DID/Unknown fallback, mixed old/new surfaces, a second Direct row, changed Persona/conversation ID, or a later name reversion fails. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/flows/profile_refresh_flow.dart`<br>evidence: `case_attestation` |
| `DISPLAY-NAME-REG-001` (active) | **Cached nickname is correct on the first visible frame**<br>Restart the App shell and open the canonical Direct row through the visible conversation list before any remote profile refresh is requested. | • After the target conversation becomes selected, the first non-empty scoped chat-header title is the exact cached nickname.<br>• The same nickname remains stable throughout the post-open observation window. | • A first visible Handle, compact/full DID, Unknown label, duplicate title widget, or later title drift fails immediately instead of waiting for self-healing. | suites: `direct, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-CANON-E2E-001` (active) | **Canonical Group conversation across App lifecycle**<br>Create the group, add the canonical peer, refresh members, restart the App shell, and send/receive the first messages. | • Group creation produces exactly one conversation whose conversation ID, canonical Group DID, and wire thread remain aligned before the first message.<br>• Adding a member, refreshing, restarting, and sending messages preserve that same single Group row. | • A disappearing empty group, duplicate Group row, changed Group identity, member-add replacement row, or first-message-only resurrection fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-E2E-001` (active) | **App group create/member/send**<br>Create a group, add the canonical CLI member, and send through visible App UI. | • A read-only resolver preflight yields a non-empty DID; the member action itself performs at most three visible search submissions, requires one exact enabled candidate whose title and avatar match the Persona Profile projection, and only then enables the selected add-member action.<br>• The selected group converges to one canonical conversation id before messaging, with the exact visible group title, empty preview and unread 0.<br>• The empty group row remains exact-one after member refresh and App restart before the first message.<br>• One canonical group and correct member exist, and the visible member-added system event uses the member nickname before Handle/DID fallback.<br>• App send reaches terminal state and CLI observes exactly one message with exact sender/content/group. | • Wrong group/member, duplicate semantic Group row, missing/disappearing empty row, title or candidate-avatar drift, Handle/DID shown despite an available nickname, direct service shortcut, nonterminal send, or duplicate message fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-E2E-002` (active) | **CLI group to App message**<br>Send a run-unique group message from CLI and navigate to it in App UI. | • Closed-view CLI delivery increments only the canonical group unread by exactly one and the visible row shows exact title, preview and unread.<br>• App renders exactly one canonical group message with exact content/sender/group and the visible sender label uses the same nickname as the group identity projection.<br>• Opening the exact row clears only that group unread and stable history requery does not duplicate the message. | • Wrong row/thread, missing/duplicate bubble, incorrect preview/unread, sender Handle/DID drift despite an available nickname, or mismatched content/identity fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-P9-001` (active) | **App structured P9 mention**<br>Compose a group mention through App UI and send it. | • The real composer retains exact typed input, opens the candidate panel from an explicit @ trigger, and after the GroupState member projection is preloaded filters to the exact enabled member in the first local frame without a loading indicator; the title and avatar match the Persona Profile projection and the composer clears only after send submission.<br>• P9 payload contains exactly the intended member DID and valid range.<br>• CLI projection preserves canonical ID, exact text and structured mention metadata. | • A per-query roster reload/loading frame, stale roster title, generic @ avatar, text-only arrival, wrong DID/range/role, malformed JSON, or duplicate message fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-P9-002` (active) | **CLI structured P9 mention**<br>Send a structured group mention from CLI to the App. | • CLI send succeeds with a canonical message id and exact application/json result type.<br>• App canonical projection preserves target DID/range and exact text.<br>• Mention renders in the correct group message exactly once. | • Substring-only matching, metadata loss, wrong target, or duplicate render fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `GROUP-REG-001` (active) | **Group history stability regression**<br>Refresh and requery the group history after bidirectional sends and mentions. | • The complete run-owned canonical message set remains exactly once and in exact App/UI order in the intended group after refresh.<br>• No run-owned group message appears in another loaded App conversation projection. | • Loaded cross-conversation leakage, missing/extra/out-of-order message, replay duplicate, or identity/content drift fails. | suites: `full, group`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-groups+awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `IDENTITY-DELETE-E2E-001` (active) | **Current credential deletion across idle Patch and cold restart**<br>After the normal cold-restart conversation assertions, delete the current credential while the conversation Patch stream is idle; then start another Flutter process against the same App state root. | • Deletion completes without waiting for presentation-stream cancellation, the native identity is absent, the active identity is not restored after cold start, and onboarding is visible without a persistent loading mask. | • A hanging Patch cancellation, retained native identity, retained active pointer, automatic relogin, same-process-only reopen, or persistent busy/loading state fails. | suites: `restart`<br>required: `release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity+awiki-im-core-dart` | `tests/e2e/flutter/desktop_cli_peer/flows/process_restart_flow.dart`<br>evidence: `case_attestation` |
| `INBOUND-FIRST-CONV-E2E-001` (active) | **Inbound-first canonical Direct creation**<br>Keep the peer conversation closed, send the first Direct message from CLI, open the resulting visible row, then re-enter through identity lookup, recents, and local search. | • The first inbound message creates exactly one resolved dm:peer-scope:v1 conversation with one peerPersonaId, exact nickname, preview, unread increment, and canonical message ID.<br>• Opening clears only that unread state, and identity lookup, recents, App-shell rebuild, and local search all reuse the same canonical conversation and exact-one timeline. | • A pre-existing Direct fixture, legacy dm:<DID> row, duplicate Persona row, wrong nickname/preview/unread, missing or duplicate message, alternate entry ID, or restart/search drift fails. | suites: `inbound`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/flows/inbound_first_flow.dart`<br>evidence: `case_attestation` |
| `MESSAGE-PATCH-RESTART-E2E-001` (active) | **Cold restart closes a message gap committed while Patch subscription is unavailable**<br>Pause Phase A, stop realtime, destroy its App ProviderContainer and native bootstrap, prove Patch/sync providers are unavailable, then commit one Direct message from the CLI and start Phase B normally. | • The gap message is accepted by the real backend only after Phase A realtime and its Patch/recovery coordinator container are destroyed.<br>• Before Phase B mounts the App, its canonical local timeline lacks the gap; the distinct process then proves subscribe-reset-sync startup order and projects the gap exactly once alongside the pre-existing timeline. | • Leaving any old Patch/pull/recovery scheduler alive, committing before container disposal, finding the gap before Phase B startup, same-process rebuild, missing startup order evidence, lost/duplicate gap, or duplicate old rows prevents pass. | suites: `restart`<br>required: `release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/flows/process_restart_flow.dart`<br>evidence: `case_attestation` |
| `MLS-MULTI-DEVICE-E2E-001` (planned) | **AWiki Me same-DID second-device MLS lifecycle**<br>Create a required-E2EE group in AWiki Me, join the CLI as the same DID, run production App MLS reconciliation, consume Welcome on the CLI, then exchange future group text and one group attachment. | • Before Welcome, the independent CLI root reports missing local MLS state and cannot see the App pre-Welcome application.<br>• The App visibly projects preparing during production reconciliation and ready only after Add, Commit, and Welcome converge.<br>• Future secure group text travels App-to-CLI and CLI-to-App exactly once.<br>• One CLI group attachment object is projected once and decrypts to the exact plaintext in AWiki Me.<br>• The same DID remains one owner business member throughout the second-device lifecycle. | • Copied native state, pre-Welcome history, stale ready during reconciliation, duplicate messages or business members, wrong attachment bytes, or MLS control/private fields in App or CLI public projections fails. | suites: `planned`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_multi_device_group_residual_ledger`<br>owner: `awiki-me-groups` | `tests/e2e/planned/multi_device_future_cases.md`<br>evidence: `case_attestation` |
| `MLS-MULTI-DEVICE-E2E-002` (active) | **AWiki Me exact-device MLS revoke convergence**<br>Revoke the CLI device through the real AWiki Me Devices UI, wait for the exact Remove and Commit convergence, then send a future secure group application from the surviving App device. | • The selected CLI device is authoritatively revoked and its exact MLS endpoint is removed.<br>• The surviving App endpoint visibly reports ready only after Remove and Commit converge.<br>• The revoked CLI endpoint is rejected at authorization before it can retrieve future group data.<br>• The App continues secure group messaging while the DID remains one owner business member. | • Removing the surviving App endpoint or DID business membership, stale ready before removal convergence, recreating the revoked endpoint, future CLI access, duplicate messages, or MLS private fields in UI/evidence fails. | suites: `step4-revoke-mls`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_multi_device_group_residual_ledger`<br>owner: `awiki-me-groups` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `MSG-E2E-001` (active) | **Direct App to CLI message**<br>Open a canonical Direct conversation, verify it before the first message and after App restart, then type and send a run-unique message through visible App controls. | • Before the first send, the resolved Persona has exactly one canonical Direct row; its visible nickname-first title, empty preview, unread 0, selected chat header, and restart state match exactly.<br>• App send reaches terminal sent state and the visible row preview is the exact sent body.<br>• CLI inbox contains exactly one canonical message with exact body, sender, receiver, thread and type. | • A second row for the same Persona, missing/duplicate empty row, title/preview drift, loss on restart, rejected or nonterminal send, wrong identity/content, zero matches, or duplicate message fails. | suites: `direct, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `MSG-E2E-002` (active) | **Direct CLI to App unread/read**<br>Send a run-unique direct message from CLI while the App target conversation is not open, then open it through UI. | • CLI send is accepted and has a canonical message ID.<br>• Global and target-conversation unread move from baseline to exactly baseline+1, while the scoped visible row shows the exact preview and localized unread count.<br>• After an App-shell rebuild, the canonical row preserves the exact title/preview/unread and one exact bubble; opening clears list and navigation unread and removes the row badge without rebound. | • Wrong conversation alias, duplicate bubble, incorrect badge delta, premature read, or rebound fails. | suites: `direct, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `MSG-REG-001` (active) | **Direct failure/retry/replay regression**<br>Inject the E2E transport failure, tap the visible retry action, reconnect, rebuild the App shell, and requery. | • The controlled failed row stays visible in the canonical conversation even when the write path uses a DID alias or a core reset arrives.<br>• The visible retry performs exactly one real remote transport attempt, reports a typed failure code if it fails, and reaches terminal sent state.<br>• The complete run-owned Direct sequence has the exact canonical IDs, contents, conversation ownership and visible order after reconnect and App-shell rebuild.<br>• Two distinct canonical messages with the same body remain exactly two ordered messages during realtime delivery, converge to strictly increasing serverSequence values, and keep that order through the stability window and rebuild. | • Silent success, duplicate/extra/missing/out-of-order message, non-increasing converged serverSequence, cross-conversation leakage, same-body collapse, new duplicate on retry/replay, lost message, or stale failed state fails immediately. | suites: `direct, full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `MSG-SEQUENCE-E2E-001` (active) | **Cross-conversation exact message sequence and isolation**<br>Deliver ordered Direct/Group traffic, then hide the App, send a three-message Direct burst without waiting for App convergence, resume, open each row, and requery local history. | • The Direct and Group run-owned canonical ids are exact-once and ordered in App projection, visible bubbles, and SDK local history with no cross-conversation leakage.<br>• After hidden/resume, the three-message burst produces exact +3 Direct/global unread and the last-message preview; opening Direct shows the exact burst ID/body/sender order and clears only that unread without rebound. | • Duplicate/missing/extra id, wrong body/sender/conversation, swapped burst order, stale preview, unread drift/rebound, or cross-conversation leakage fails. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |
| `MULTI-DEVICE-CAPABILITY-GATE-E2E-001` (active) | **AWiki Me default device entry and independent security gates**<br>Open one temporary Storage Scope through production AppBootstrap/native Core, verify the default device-management composition, and open the public onboarding Join entry without submitting an OTP. | • The launch uses a real Storage Scope ID, and the temporary App root is deleted after native runtime disposal.<br>• With default configuration the production device-management adapter and public Join entry exist.<br>• The public entry opens the real Handle/phone/OTP Join form, while root transfer, revoke, Direct multi-device, and group multi-device remain closed behind independent security gates; Handle Recovery has no executable V1 flow. | • A reused/copied Core root, fake provider, backend-dependent oracle, a missing Join entry hidden behind the removed broad gate, hidden enablement of a high-risk capability, OTP submission, or any claim that remote Join/SAS/Root/Recovery passed fails the case. | suites: `multi-device`<br>required: `pull_request, release`<br>env: `no_service`<br>cleanup: `test_owned_temporary_scope_delete`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_capability_gate_test.dart`<br>evidence: `case_attestation` |
| `NATIVE-E2E-001` (active) | **Storage Scope native bootstrap smoke**<br>Provision one Storage Scope through the real App bootstrap, reopen the same native vault, then remove its key and retry openExisting. | • Provision reaches ready only after the native VaultRequired validator opens.<br>• A second real App bootstrap reads the identical existing root.<br>• After key deletion, bootstrap returns vault_key_missing and does not recreate the item. | • Native load failure, context/path mismatch, root replacement, or missing-key recreation fails the case. | suites: `smoke`<br>required: `pull_request, release`<br>env: `no_service`<br>cleanup: `none`<br>owner: `awiki-me` | `tests/e2e/flutter/native/im_core_open_smoke_test.dart`<br>evidence: `case_attestation` |
| `NATIVE-E2E-002` (active) | **Signed production Keychain process restart**<br>Provision one unique production scope item, terminate the test App process, launch a separately built process to reopen it, reject duplicate create, then delete it in a third process. | • The provision process creates revision 1 only in the production service.<br>• The independent reopen process reads the same revision and createExclusive reports already-exists.<br>• The cleanup process deletes the unique item and confirms missing. | • Ad-hoc identity, dev-service access, restart read failure, silent replacement, or cleanup failure fails the release gate. | suites: `production-keychain`<br>required: `release`<br>env: `no_service`<br>cleanup: `unique_scope_item_delete`<br>owner: `awiki-me-identity` | `scripts/run_macos_production_scope_restart_gate.sh`<br>evidence: `signed_process_command_log` |
| `PERF-E2E-001` (active) | **Real backend performance flow**<br>Complete the measured App/CLI backend scenario. | • All required timing phases complete against the remote product path. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-002` (active) | **Multi-conversation dataset**<br>Prepare the audited remote dataset. | • Dataset cardinality and conversation distribution match the plan. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-003` (active) | **Cold shell visibility**<br>Measure cold App shell and conversation-list visibility. | • Both cold visibility metrics are present and finite. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-004` (active) | **Snapshot/hydration timing**<br>Measure snapshot, fast-local and full hydration. | • All hydration metrics are present in causal order. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-005` (active) | **App→CLI latency**<br>Measure App send to CLI visibility. | • Latency uses the run-owned canonical message and is within the hard budget. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-006` (active) | **CLI→App latency**<br>Measure CLI send to App visibility. | • Latency uses the run-owned canonical message and is within the hard budget. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-007` (active) | **No full-refresh regression**<br>Observe refresh counters during send/receive. | • No forbidden full conversation refresh occurs. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-008` (active) | **Long-thread open timing**<br>Open and hydrate the long audited thread. | • Open/load metrics are present and within the hard budget. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-009` (active) | **Timing report schema**<br>Write the product timing report. | • Required metrics, units, source and run identity are complete. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-010` (active) | **Hard budget semantics**<br>Evaluate hard performance budgets. | • A hard-budget breach makes the suite fail. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-011` (active) | **Soft budget semantics**<br>Evaluate soft performance budgets. | • A soft breach is recorded as warning without hiding hard failures. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERF-E2E-012` (active) | **Performance diagnostics**<br>Retain sanitized failure diagnostics. | • Diagnostics identify the metric/budget without secrets or local paths. | • Missing metric, wrong canonical sample, schema drift, or incorrect budget classification fails. | suites: `performance`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-performance` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `timing_report+case_attestation` |
| `PERSONALAGENT-E2E-001` (active) | **Personal Agent enable UI**<br>Select the daemon and enable Personal Agent through visible UI. | • Daemon selection, active binding and enabled UI agree. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `personal-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/personal_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `PERSONALAGENT-E2E-002` (active) | **Personal Agent receive/process**<br>Send from CLI and wait for App/runtime recovery. | • Exact source message and one runtime final result are visible in App history. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `personal-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/personal_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `PERSONALAGENT-E2E-003` (planned) | **Personal Agent confirmation/result**<br>Confirm the visible draft/action and await its result. | • Confirmation is attributable to the canonical request and one final result is returned. | • Missing/disabled UI, skipped real scenario, wrong runtime identity, duplicate result, or backend/UI divergence fails. | suites: `planned`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/personal_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `PERSONALAGENT-E2E-004` (active) | **Personal Agent revoke lifecycle**<br>Revoke Daemon message authorization through the visible confirmation flow. | • User Service binding and daemon binding both converge to revoked after the UI action. | • UI-only success, one-sided revoke, missing confirmation, or stale active binding fails. | suites: `personal-agent`<br>required: `optional_nightly`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-agents` | `tests/e2e/flutter/app/personal_agent_full_ui_test.dart`<br>evidence: `case_attestation` |
| `PROCESS-RESTART-E2E-001` (active) | **Canonical conversation cold-process restart**<br>Phase A creates Direct/Group conversations, messages, unread state, and cached Profiles; Phase B starts a new Flutter process and restores only persisted state. | • The second process restores Persona/Group semantic exact-one, exact message order, unread state, and cached first-visible names without reusing in-memory providers. | • Same-process Widget rebuild, duplicate/lost rows, unread drift, message replay, Handle/DID flash, changed native content hash, or a different state root fails. | suites: `restart`<br>required: `release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging+awiki-me-identity` | `tests/e2e/flutter/desktop_cli_peer/flows/process_restart_flow.dart`<br>evidence: `case_attestation` |
| `ROOT-TRANSFER-E2E-001` (active) | **AWiki Me management-device root import**<br>Open Devices on the ready AWiki Me admin, select the exact eligible member, let Core prepare a safe recipient summary, confirm operating-system user presence once, stop the App sender flow at standard P5 accepted, then bring the CLI receiver online to import and complete readiness directly. | • Before receiver completion, the exact recipient remains active member with management_ready false; no Init-only pending, sender waiting, or App-owned transfer state is projected.<br>• The App displays only Core's safe recipient summary, delegates exactly one real user-presence check, and treats the closed standard P5 accepted receipt as the end of the sender operation.<br>• The CLI receiver imports the envelope, completes directly with User Service, and becomes active admin with management_ready true after fresh device auth and Registry convergence; Root control never appears in either product inbox. | • A wrong or ineligible target, caller-supplied message ID, old list/retry/pending state, a second user-presence action, treating accepted as receiver readiness, waiting for or emitting an imported ACK/Reply, root/control JSON in inbox or UI, Root bytes in logs/errors/reports, or management actions before Registry readiness fails. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-identity` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `ROOT-TRANSFER-E2E-002` (planned) | **Receiver-local root completion recovery**<br>Restart only the receiving device, let Core resume its local HTTPS completion coordinator, obtain fresh device auth, read Registry once, and promote the already sealed pending root after authoritative admin-ready convergence. | • Receiver-local recovery is idempotent across response loss, produces one readiness transition, projects ready only from fresh auth plus Registry, and promotes the pending root without any sender participation. | • Sender retry, a second user-presence prompt, an imported ACK/Reply, public original-message-ID retry semantics, duplicate readiness generation, a second App-owned state store, false ready before Registry, or raw transport error text fails. | suites: `planned`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_device_join_session_and_identity_cleanup`<br>owner: `awiki-me-identity` | `tests/e2e/planned/multi_device_future_cases.md`<br>evidence: `case_attestation` |
| `SMOKE-E2E-001` (active) | **App shell smoke**<br>Launch the real Flutter desktop app shim without backend credentials. | • App shell and onboarding are visible exactly once.<br>• The onboarding authentication entry is visible; macOS exposes the single phone, email, and identity-credential method selector.<br>• On macOS, the phone OTP form submits the same-step handle and its tenant-qualified full handle.<br>• Unauthenticated realtime remains disconnected. | • A missing shell/onboarding/authentication-entry surface, a dropped OTP handle scope, or an unexpected realtime connection fails the case. | suites: `smoke`<br>required: `pull_request, release`<br>env: `no_service`<br>cleanup: `none`<br>owner: `awiki-me` | `tests/e2e/flutter/app/app_smoke_test.dart`<br>evidence: `case_attestation` |
| `STEP4-GROUP-PAGINATION-E2E-001` (active) | **AWiki Me explicit group inventory pagination**<br>Read the first group page with limit one, retain its opaque cursor, and explicitly request the next page. | • The first page has one item, has_more and a non-empty opaque cursor; the explicit next request returns a distinct stable group projection. | • Offset pagination, cursor parsing, automatic unbounded collection, duplicate items, or fake service data fails. | suites: `step4-revoke-mls`<br>required: `optional_nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `isolated_multi_device_group_residual_ledger`<br>owner: `awiki-me-groups` | `tests/e2e/flutter/app/multi_device_join_ui_test.dart`<br>evidence: `case_attestation` |
| `UNREAD-MULTI-E2E-001` (active) | **Multi-conversation unread isolation and read clearing**<br>Receive two Direct messages and one Group message, restart, open Group, then open Direct. | • Before opening, Direct unread is exactly 2, Group unread is exactly 1, and global unread increases by exactly 3.<br>• Opening Group clears only Group and leaves Direct at 2; opening Direct then restores the exact baseline total with no rebound during the stability window. | • Wrong per-row/global count, clearing the wrong row, lost restart state, duplicate badge, or delayed unread rebound fails. | suites: `full`<br>required: `nightly, release`<br>env: `awiki_info_remote`<br>cleanup: `fixed_account_pool_residual_ledger`<br>owner: `awiki-me-messaging` | `tests/e2e/flutter/desktop_cli_peer/desktop_cli_peer_e2e.dart`<br>evidence: `case_attestation` |

## Known coverage boundaries

- `full` means the audited Direct, Group, P9 Mention, Contacts and Attachment product slices plus one real App-admin/CLI-member Join and `ROOT-TRANSFER-E2E-001` readiness-completion lifecycle. It does **not** claim onboarding beyond that isolated lifecycle, profile editing/search, identity switching, group role/remove/leave, secure-trust UI, mobile-device, or optional runtime-provider coverage.
- `performance` is an integration diagnostic because it prepares data through application services; it is not required UI acceptance.
- `multi-device` is currently a no-service device-entry E2E only. It opens an isolated production AppBootstrap/native Core scope, checks the default device-management composition, and opens the public Join form. It does not claim OTP, SAS, approval, root import, revoke, MLS, or Handle Recovery acceptance.
- `multi-device-remote-join` is a separate, operator-confirmed `awiki.info` suite for `DEVICE-JOIN-E2E-001/002` only. It runs both App-new-device/CLI-admin and App-admin/CLI-new-device directions with independent native roots, dynamically resolved one-time OTPs, SAS comparison, pending-session App restart coverage, the production CLI foreground contract, fixed member authorization, CLI listener host wake, App global review entry, and real macOS user presence where the App approves. The tests do not directly call Message Inbox hydration, requestSync(), or refreshJoinInbox() to discover Join. It does not execute root transfer, revoke, or MLS; the root lifecycle is registered by `full`. A checked-in implementation is not evidence of a remote pass while the dedicated account allowlist or operator prerequisites are unavailable.
- `multi-device-app-pair` is the one-host/two-process App↔App mode. It builds two Debug bundles with stable distinct bundle identifiers, isolated Flutter build directories, and independent native Core state roots, then concurrently drives their visible Join UI. The loopback coordinator carries only lifecycle checkpoints and compares transient SAS values in memory; it cannot call product APIs or write SAS evidence. This mode currently covers only `DEVICE-JOIN-E2E-004` and retains real macOS user presence.
- `multi-device-app-pair-functional` reuses the same two isolated App processes but injects an E2E-only `UserPresencePort` so Agent inventory and Direct-message convergence can run unattended. It covers one shared Daemon plus Codex/Claude runtime projection, bidirectional App outbound own-sync, and one required-secure CLI peer reply on both Apps. It does not attest LocalAuthentication and never changes the production user-presence implementation.
- `DEVICE-JOIN-E2E-003`, `ROOT-TRANSFER-E2E-002`, and `MLS-MULTI-DEVICE-E2E-001` are planned, non-executable boundaries. `ROOT-TRANSFER-E2E-001` is active in its dedicated suite; `DEVICE-REVOKE-E2E-001` and `MLS-MULTI-DEVICE-E2E-002` are active in `step4-revoke-mls`. A later version must supply accepted contracts, independent device roots, real one-time OTPs, user presence where required, scenario-owned oracles, cleanup, and explicit suite registration. `ROOT-TRANSFER-E2E-002` is receiver-local completion recovery only; it cannot restore sender retry, imported ACK/Reply, a second user-presence prompt, or public original-message-ID semantics. `001` covers one pending App restart; standalone cancellation and the real expiration path remain fail-closed rather than adding a test-only TTL or time bypass.
- Personal Agent, Codex and Claude Code remain `optional_nightly`. A missing provider/configuration is reported as skipped/not-run, never passed.
- `PERSONALAGENT-E2E-003` is cataloged as planned, not executable: the supporting confirmation/draft step does not yet have its own accepted case attestation. The runnable Personal Agent suite attests enable, receive/process and exact revoke convergence.
- The latest recorded `awiki.info` conversation-correctness evidence includes a focused inbound-first run and a full Direct/Contacts/Group/Unread/Ordering/Display-name run, a focused full-Handle fallback run, plus a release-only two-process cold restart run. The full-Handle run currently exposes a real App-visible generated-name fallback failure. The v7 Full hidden/resume burst also exposes a real App-visible canonical message-order failure in `MSG-SEQUENCE-E2E-001`. The separate DID-only actor and Runtime Agent entry remain explicit planned boundaries.

## Validation

```bash
dart run tool/validate_test_catalog.dart
dart run tool/validate_test_catalog.dart --report <suite-report.json>
```
