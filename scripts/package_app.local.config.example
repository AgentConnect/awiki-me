# Local package-controller overrides. This file is Git-ignored.
# macOS signing and notarization credentials belong only in the protected
# app-packaging GitHub Environment; package_app.sh dispatches CI and never
# reads a local P12 or API private key.

# Optional focused validation target override. Leave this unset to keep the
# committed four-platform release target set. A subset still uses the same
# GitHub workflow, but is saved under
# dist/validation/<version>+<build>/<request-id>/ with a validation package
# manifest. It does not generate or replace any latest.json.
# PACKAGE_TARGETS="windows-x64"

# Optional complete replacement for the two built-in tenants. Relative paths
# are resolved from the repository root; omitted means the committed default.
# PACKAGE_TENANT_CONFIG="/absolute/path/to/builtin-tenants.json"
